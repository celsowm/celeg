# Domain test targets are registered from tests/Targets.cmake after the
# production backend targets exist. Keep this early manifest limited to shared
# test-root metadata so ownership stays with each domain manifest.
set(CELEG_TESTS_ROOT tests)
